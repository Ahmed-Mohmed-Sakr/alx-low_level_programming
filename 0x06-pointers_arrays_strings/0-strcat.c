#include "main.h"

/**
 * _strcat - function that concatenates two strings.
 *
 * @dest: first string
 * @src: second string.
 *
 * Return: string
 */

char *_strcat(char *dest, char *src)
{
	srtcat(dest, src);

	return (dest);
}
