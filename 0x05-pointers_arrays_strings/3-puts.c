#include "main.h"

/**
 * _puts -  function that prints a string, followed by a new line, to stdout.
 *
 * @str: pointer to string
 *
 * Return: void
 */

void _puts(char *str)
{
	printf("%s\n", str);
}
