#include "main.h"

/**
 * _strncat - just prototype function to test dynamic lib.
 *
 * @dest: facke value.
 * @src: fack value.
 * @n: fack vale.
 *
 * Return: fack value
 */
char *_strncat(char *dest, char *src, int n)
{
	(void)dest;
	(void)src;
	(void)n;

	return (NULL);
}
