#include "main.h"

/**
 * _strcat - just prototype function to test dynamic lib.
 *
 * @dest: facke value.
 * @src: fack value
 *
 * Return: fack value
 */
char *_strcat(char *dest, char *src)
{
	(void)dest;
	(void)src;

	return (NULL);
}
