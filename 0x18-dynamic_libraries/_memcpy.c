#include "main.h"

/**
 * _memcpy - just prototype function to test dynamic lib.
 *
 * @dest: facke value.
 * @src: fack value.
 * @n: fack vale.
 *
 * Return: fack value
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	(void)dest;
	(void)src;
	(void)n;

	return (NULL);
}
