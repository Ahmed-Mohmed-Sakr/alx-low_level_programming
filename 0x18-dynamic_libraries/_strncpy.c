#include "main.h"

/**
 * _strncpy - just prototype function to test dynamic lib.
 *
 * @dest: facke value.
 * @src: fack value.
 * @n: fack vale.
 *
 * Return: fack value
 */
char *_strncpy(char *dest, char *src, int n)
{
	(void)dest;
	(void)src;
	(void)n;

	return (NULL);
}
