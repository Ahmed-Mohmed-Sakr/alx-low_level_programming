#include "main.h"

/**
 * _memset - just prototype function to test dynamic lib.
 *
 * @dest: facke value.
 * @src: fack value.
 * @n: fack vale.
 *
 * Return: fack value
 */
char *_memset(char *dest, char src, unsigned int n)
{
	(void)dest;
	(void)src;
	(void)n;

	return (NULL);
}
